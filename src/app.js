/*$(window).on('resize', function() {
	var container = $('#topDetail');
	var height = $(window).height();
	var imgHeight = $("#heroSection").height();

	if(imgHeight > height){
		container.css({marginTop: (height-80)});
	}
	else{
		container.css({marginTop: (imgHeight-80)});
	}

}).trigger('resize');*/